[
  {"id":"s1","category":"protocol-facts","question":"In the Verb set and wire format architecture note, what default network port and ALPN identifier does Mark use? Return the port as a number and the ALPN identifier without quotation marks.","fields":{"port":"number","alpn":"string"}},
  {"id":"s2","category":"default-policy","question":"According to ADR 0003, what exact OKF type is supplied for a concept document when its publisher omits the type?","fields":{"type":"string"}},
  {"id":"s3","category":"licensing","question":"Under ADR 0009, which licenses apply to the core implementation, plugins, and protocol specification? Return their license identifiers.","fields":{"core":"string","plugins":"string","specification":"string"}},
  {"id":"s4","category":"supersession-multi-document","question":"Using ADR 0006 and its successor, identify the ADR number that superseded the optional Postgres build decision, and whether that successor removed the Postgres backend. Return the successor number as an integer.","fields":{"successor":"number","removed":"boolean"}},
  {"id":"s5","category":"response-contract","question":"Under ADR 0012, what is the default LOOKUP match mode, and which exact response key/value echo proves that a response actually used body matching?","fields":{"default_mode":"string","body_echo":"string"}},
  {"id":"s6","category":"oauth-policy","question":"Under ADR 0011, must native-scheme redirect allowlisting match the entire URI rather than only its scheme, and can operators add callbacks through a configuration knob?","fields":{"full_uri_match":"boolean","configuration_knob":"boolean"}},
  {"id":"s7","category":"architecture-roles","question":"In the Universe Pattern, what are the exact role names for a team's server, an indexing server, and developer-local persistent agent memory? Use the heading names.","fields":{"team":"string","indexing":"string","memory":"string"}},
  {"id":"s8","category":"historical-revision","question":"In version 3 of ADR 0005, what old and new graph.json schemaVersion numbers are specified for the accept-both migration? Use that historical revision, not the current promoted summary.","fields":{"old_version":"number","new_version":"number"}}
]
