// Package broker is the demarkus OIDC token broker: SSO in front of demarkus
// worlds, one long-lived write token per world, and the MCP gateway. Run is
// the shared main; the surfaces live in core, oauthsrv, gateway and storage.
package broker
