version: 2

project_name: demarkus-tools

monorepo:
  tag_prefix: tools/
  dir: tools

builds:
  - id: demarkus-broker
    main: ./demarkus-broker
    binary: demarkus-broker
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
      - arm
    goarm:
      - "7"
    ldflags:
      - -s -w

  - id: demarkus-token
    main: ./demarkus-token
    binary: demarkus-token
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
      - arm
    goarm:
      - "7"
    ldflags:
      - -s -w

  - id: demarkus-publish
    main: ./demarkus-publish
    binary: demarkus-publish
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
      - arm
    goarm:
      - "7"
    ldflags:
      - -s -w

# Three separate archives so operators pulling only the admin CLI they
# need don't carry the full bundle. Same shape as client/.goreleaser.yml.
archives:
  - id: demarkus-broker
    builds:
      - demarkus-broker
    name_template: "demarkus-broker_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

  - id: demarkus-token
    builds:
      - demarkus-token
    name_template: "demarkus-token_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

  - id: demarkus-publish
    builds:
      - demarkus-publish
    name_template: "demarkus-publish_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: "demarkus-tools_checksums.txt"

changelog:
  use: github-native
