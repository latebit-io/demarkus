// Package storefmt is the stored document model every backend shares: the
// version file format, publisher metadata rules, path canonicalization, the
// document types and the error sentinels. It does no I/O; the filesystem
// backend lives in protocol/store.
package storefmt
