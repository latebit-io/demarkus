# Deployment (Operations)

Operator-facing deployment documentation that is not part of the docs site's guided pages.

- [Observability](observability.md) — structured-log field schemas for `demarkus-server`, `demarkus-knowledge-server`, `demarkus-broker`, and `demarkus-agent`, plus log-shipper pointers.

Guided deployment pages (TLS, single host, appliance, Kubernetes & Helm) live in [docs/site/deployment/](../site/deployment/index.md).
